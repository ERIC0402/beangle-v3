[#ftl]
<div class="search_item">
	<input type="text" class="form-control input-medium" id="${tag.id}" name="${tag.name}" alt="${tag.label}"  placeholder="${tag.label}" maxlength="${tag.maxlength}" value="${tag.value!}" ${tag.parameterString}/>
</div>